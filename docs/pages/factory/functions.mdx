---
title: Keep Factory
description: Events emitted by Keep Factory
---

import KeepFactory from '../../../out/KeepFactory.sol/KeepFactory.json'; 
import Function from '../../src/Function';

# Functions
{KeepFactory.ast.nodes.filter(node => node.nodeType == "ContractDefinition")[0].nodes.filter(node => node.nodeType == "FunctionDefinition").map(func => <Function func={func} />)}
