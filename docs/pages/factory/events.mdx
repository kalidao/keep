---
title: Keep Factory
description: Events emitted by Keep Factory
---

import KeepFactory from '../../../out/KeepFactory.sol/KeepFactory.json'; 
import Event from '../../src/Event';

# Events
{console.log(KeepFactory.ast.nodes.filter(node => node.nodeType == "ContractDefinition")[0].nodes.filter(node => node.nodeType == "EventDefinition"))}
{KeepFactory.ast.nodes.filter(node => node.nodeType == "ContractDefinition")[0].nodes.filter(node => node.nodeType == "EventDefinition").map(event => <Event key={event.name} event={event} />)}
