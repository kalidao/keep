---
title: Keep Token Errors
description: Errors emitted by Keep
---

import KeepToken from '../../../out/KeepToken.sol/KeepToken.json'; 
import Error from '../../src/Error';

# Errors
{KeepToken.ast.nodes.filter(node => node.nodeType == "ContractDefinition")[1].nodes.filter(node => node.nodeType == "ErrorDefinition").map(error => <Error key={error.name} error={error} />)}
