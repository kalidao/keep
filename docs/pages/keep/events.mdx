---
title: Keep
description: Events emitted by Keep
---

import Keep from '../../../out/Keep.sol/Keep.json'; 
import Event from '../../src/Event';

# Events

{Keep.abi.filter(event => event.type == "event").map(event => <Event key={event.name} name={event.name} inputs={event.inputs} />)}
