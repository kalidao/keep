---
title: Keep
description: Events emitted by Keep
---

import Keep from '../../../out/Keep.sol/Keep.json'; 
import Event from '../../src/Event';

# Events

{console.log(Keep.ast.nodes.filter(node => node.nodeType == "ContractDefinition")[0].nodes.filter(node => node.nodeType == "EventDefinition"))}
{Keep.ast.nodes.filter(node => node.nodeType == "ContractDefinition")[0].nodes.filter(node => node.nodeType == "EventDefinition").map(event => <Event key={event.name} event={event} />)}
