---
title: Keep
description: Events emitted by Keep
---

import Keep from '../../../out/Keep.sol/Keep.json'; 
import Variable from '../../src/Variable';

# Variables

{console.log(Keep.ast.nodes.filter(node => node.nodeType == "ContractDefinition")[0].nodes.filter(node => node.nodeType == "VariableDeclaration"))}
{Keep.ast.nodes.filter(node => node.nodeType == "ContractDefinition")[0].nodes.filter(node => node.nodeType == "VariableDeclaration").map(variable => <Variable key={variable.name} variable={variable} />)}
